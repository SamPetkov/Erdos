# Exact theorem brief

This file is a gate, not a suggestion. Antigravity, Gemini, AXLE, and
Aristotle must not infer a stronger, weaker, or merely similar target.

## Identity

- Brief ID:
- DAG node:
- Private repository:
- Frozen private commit SHA:
- Exact declaration:
- Source file:
- Signature SHA-256:

## Verbatim Lean target

```lean
-- Paste the complete declaration through the final type, excluding `:=`.
```

## Mathematical contract

- Definitions and notation:
- Quantifiers and parameter ranges:
- Assumptions and where each is used:
- Desired conclusion:
- Dependency nodes already validated:
- Edge cases and limiting cases:

## Bounded task

- Current Lean error or mathematical obstruction:
- Minimal candidate lemma:
- Permitted files:
- Permitted imports and prior theorems:
- Non-goals:

## Forbidden shortcuts

- `sorry`, `admit`, project-specific axioms, `unsafe`, `native_decide`, or
  `implemented_by`;
- statement weakening, hidden hypotheses, circular dependencies, or an
  equivalently hard result disguised as a helper lemma;
- unrelated refactoring or any public-repository write.

## Acceptance evidence

- warning-fatal Lean replay under the repository's pinned toolchain;
- exact statement and signature-hash match;
- placeholder/escape-hatch scan;
- `#print axioms` with only standard Lean/Mathlib axioms;
- independent mathematical review, including adversarial edge cases;
- standalone/self-contained regeneration and checks when the dependency
  closure changes.

## Authorization

- User approval record:
- Aristotle authorization: **NOT APPROVED**

Only the user may approve the exact brief. An agent may draft or revise this
file, but must not change the authorization line on the user's behalf.
