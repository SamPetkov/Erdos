# Codex review loop

Always follow `@/AGENTS.md`.

When working on a branch associated with a private pull request, treat the
latest unresolved `CODEX-DIRECTIVE v1` PR comment as the active bounded task.
Read it before editing, acknowledge it, and return an evidence-bearing
`ANTIGRAVITY-REPORT v1`.

The private PR is not append-only. Correct existing files when that is the
faithful fix, including modifying, replacing, renaming, or deleting prior work.
Do not touch or merge any public repository. Do not submit Aristotle work
without the exact user-approved theorem brief required by `AGENTS.md`.
