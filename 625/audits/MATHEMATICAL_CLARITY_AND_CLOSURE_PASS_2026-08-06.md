# Erdős 625 mathematical clarity and closure pass

**Date:** 2026-08-06  
**Branch:** `agent/625-referee-readable-tier-one-pass`  
**Scope:** Sections 5, 7, 8, 9, final constant ledger, and manuscript validation  
**Status:** fail-closed; this audit does not promote the main theorem

## Executive assessment

This pass addresses five places where a referee could reasonably object that a correct idea had not yet been expressed at the theorem-facing level:

1. Section 5 moved from the finite target mean at the unrestricted root to the phase center inside an unexplained `o(1)`;
2. the central partial-diagonal proof compressed the uniform Stirling and entropy remainder into prose;
3. Section 8 could be misread as deleting overlaps with common whole classes rather than using partial diagonals as a reference measure;
4. Section 9 defined residual activities by dividing by the residual mass before separating the zero-residual case;
5. the final displayed coefficient relied on a strict phase certificate without exhibiting a fixed margin capable of absorbing the deterministic `o(1)` error.

All five interfaces are now explicit. The project remains in proof closure, not at a completed-proof boundary.

## 1. Finite-target transport in the signed root separation

The root comparison is now isolated in
`625/arxiv/SECTION5_ROOT_TRANSPORT_V3.tex`. At the unrestricted root, the manuscript first records the exact finite identity

```text
Phi_n(r_+)
  = r_+ {log 2 - D_{4,n}(T_+(n))},
T_+(n) = alpha - n/r_+(n).
```

No limiting functional is substituted in this equality. The passage to the phase center is then decomposed into two uniform errors:

1. the uniform convergence of the finite dual values on the compact target interval `K_*`;
2. the displacement `T_+(n)-T_0 = O(log log n/log n)`.

The limiting dual derivative is

```text
F_S'(T) = -lambda_S(T),
```

and the tilts are uniformly bounded on `K_*`. Hence the two limiting dual values are uniformly Lipschitz. The manuscript defines one deterministic error sequence

```text
omega_n^root
  = sup |F_{n,S+}-F_{S+}|
    + sup |F_{n,S4}-F_{S4}|
    + C |T_+(n)-T_0|,
```

proves `omega_n^root -> 0` uniformly in the phase, and obtains

```text
Phi_n(r_+)
  = r_+ {log 2 - D_4(delta) + O(omega_n^root)}.
```

The positive entropy margin and the positive corridor derivative are then used explicitly to prove

```text
r_4^co < r_+,
```

so the orientation and nonemptiness of the subsequent mean-value interval are no longer implicit.

This substantially improves E625-08/E625-10 readability. It does not close either node until the concrete theorem package and its Lean-facing signature are frozen and replayed.

## 2. Uniform central partial-diagonal extraction

The central argument is now isolated in
`625/arxiv/SECTION7_CENTRAL_EXTRACTION_V3.tex` instead of being generated by a long opaque prose substitution. The proof starts from the exact factorial identity and records the following chain.

### Uniform Stirling remainder

For every nonnegative factorial argument,

```text
log(t!) = t log t - t + sigma(t),
|sigma(t)| <= C_S log(t+2).
```

Only a fixed number of factorials occurs because the support has four coordinates. Hence the total error is `O(log n)` uniformly, including when a selected or residual coordinate vanishes.

### Entropy boundary estimate

The exact rounded profile has all four proportions uniformly bounded below. The coordinatewise entropy expression is bounded by

```text
C_H y log(e/y),
```

and the four-coordinate entropy inequality gives

```text
sum_i y_i log(e/y_i) <= Y log(4e/Y).
```

Thus no differentiability at a zero coordinate is assumed.

### Affine phase extraction

Exact subtraction of consecutive `E_i` values gives

```text
E_{i+1} - E_i = -(log 2) alpha / 2 + O(1).
```

Averaging after adding the affine correction isolates the only term needed from the positive complete first moment: a phase-uniform upper bound on the profile average `bar E`. This yields

```text
sum_i y_i E_i
  <= ((log 2) alpha / 2)(T Y - I) + O(Y).
```

### Residual-fraction transport

The proof now displays

```text
rho - R = (I - T Y)/(alpha - T),
|I - T Y| <= 3Y,
```

and bounds the difference between `n rho log rho` and
`k_co alpha R log R` by `O(k_co Y)`. This is the missing bridge between the exact factorial variable and the scalar rate variable.

### Deterministic error ratio

The central summation now defines one deterministic phase-uniform error ratio

```text
epsilon_n^diag
  = C log(2e/eta)/alpha
    + 2C log n/(k_co alpha eta),
```

proves `epsilon_n^diag -> 0`, and obtains

```text
D(ell) <= exp(-c k_co log log n)
```

uniformly over the entire central range. The polynomial number of four-coordinate subprofiles is then negligible.

### Remaining status

This is now a complete candidate paper proof of the central extraction. E625-11B remains `needs-review`, rather than `welded`, until the exact theorem signature is frozen, the argument is independently checked line by line, and the corresponding Lean theorem is replayed with the actual midpoint-profile hypotheses.

## 3. Section 7–8 accounting interface

Section 7 does not remove, condition away, or subtract overlaps containing common whole classes. Its partial-diagonal weights form a one-sided normalized reference sum. Section 8 dominates endpoint-table weights by this reference sum through the square-free transport inequality.

The manuscript now states this explicitly at the start and end of Section 8. It also defines

```text
w_hi(P,j) := w(P,j)
```

so the bare high-skeleton weight in Section 8 is visibly the same object used in the exact conditional decomposition of Section 9.

The domain of the one-sided reference vector is now stated as the finite coordinate box `0 <= r_i <= k_i`. This avoids an implicit extension of the partial-diagonal weights outside their combinatorial domain.

## 4. Zero residual mass and exact conditional decomposition

The residual activity

```text
theta_ab = e d_a d'_b / m_0
```

is meaningful only for `m_0 > 0`. Section 9 now separates the case `m_0=0` first. The residual matching is uniquely empty, all residual local factors are one, and the exposed high cells form a matching, so their cycle rank is zero. Therefore

```text
A(M,j) = 1  when m_0 = 0.
```

The positive-residual activity proof is then stated under `m_0>0`.

The exact overlap decomposition is also justified by a two-way reconstruction:

```text
overlap matrix
  <-> canonical high-cell matching and multiplicities
      + capped residual matching with no return to exposed cells.
```

This makes uniqueness and absence of double counting explicit. The prescribed-cell bound is described as a joint factorial-moment inequality; the manuscript now says directly that no independence between residual cells is asserted.

## 5. Fixed slack in the final constant

The former certificate established a strict bound of the form

```text
A_4(delta) > log(1000/639),
```

but the phase-resolved probability bound subtracts a deterministic error `epsilon_n -> 0`. A strict inequality alone does not display a uniform numerical margin.

The exact rational checker now verifies the sharper bounds

```text
L(49q/20) < 2629/10000,
H(29q/10) < 37/2500,
L(29q/10) < 329/2500,
H(83q/20) < 357/2500.
```

Both tilt ranges therefore satisfy

```text
L(lambda_4) + H(lambda_4) < 2777/10000,
```

which gives

```text
A_4(delta) > log(20000/12777).
```

The exact fixed slack over the displayed theorem constant is

```text
log(20000/12777)
  = log(1000/639) + log(12780/12777).
```

The final proof now explicitly takes `n` large enough that the deterministic error is smaller than the coefficient corresponding to this positive slack. This closes a real logical presentation gap in the final constant ledger without changing the stated theorem coefficient.

## 6. Midpoint rounding consistency

The total number of classes is fixed first as

```text
k_co = ceil((r_4^co + r_+)/2).
```

The tangent correction changes only the four type multiplicities and preserves both conservation laws. It does not add another bounded correction to `k_co`. The final assembly has been synchronized with Section 5 accordingly; the former extra `+b_n` notation has been removed.

## 7. PDF and validation hardening

Manually tagged displays are now placed in unnumbered AMS environments. This prevents the equation counter from generating repeated Hyperref destinations. The PDF workflow now fails if the final log contains

```text
destination with the same identifier
```

in addition to its existing unresolved-reference, unresolved-citation, and overfull-box gates.

The fail-closed checker now requires:

- the standalone Section 5 root-transport source;
- the finite identity, target displacement, Lipschitz argument, and deterministic root-error sequence;
- the standalone central extraction source;
- the uniform Stirling, entropy, transport, and deterministic diagonal-error markers;
- the Section 8 reference-measure wording and `w_hi` identification;
- the Section 9 zero-residual identity and no-independence statement;
- the stronger exact constant and explicit slack;
- absence of an untracked `b_n` correction.

The workflow archives both theorem-facing source files alongside the generated body and renders representative pages from Sections 5 and 7.

## Remaining proof-closure boundary

This pass does not close the following nodes:

1. the exact concrete phase center and slope package;
2. the full-sequence chromatic lower tail;
3. the complete signed four-size first-moment assembly;
4. the phase-uniform empty-corner activity asymptotic;
5. independent verification and Lean formalization of the central extraction;
6. the phase-uniform full-corner local-ratio asymptotic;
7. the normalized disjoint three-range partial-diagonal assembly;
8. the complete high-skeleton quotient and endpoint asymptotic theorem;
9. the global residual attachment and normalized-second-moment theorem;
10. the final integrated theorem-facing replay.

Accordingly, the publication switch must remain disabled and PR #58 must remain a draft.
